<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% puts "ATTENTION ATTENTION here we go...." %>
<% puts "trying to send eMail with  USERID:" + ENV["GMAIL_USERNAME"] + " PASSWORD:"+ ENV["GMAIL_PASSWORD"]%>
<% puts "ATTENTION ATTENTION here we go...." %>

<% UserMailer.signup_confirmation(User.find(current_user.id)).deliver_now %>