
    <%= form_for @bid do |f| %>

        <% if @bid.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>
              <ul>
                  <% @bid.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
              </ul>
            </div>
        <% end %>

        <div class="row">
            <div class="col-md-12">
              <div class="panel panel-heading header">
                <li_header>
                <%= link_to @bid do %>
                  <i class="btn btn-primary btn-lg glyphicon glyphicon-list"></i>
                <% end %>
                AUSSCHREIBUNG
                </li_header>
              </div>
            </div>
        </div>

        <div class="row">
                <div class="col-md-6">
                  <div class="panel-body">
                    <div class="form-group">
                      <%= f.label "Bezeichnung" %><br/>
                      <%= f.text_field :name, autofocus: true, class:"form-control"%>
                    </div>
                    <div class="form-group">
                      <%= f.label "Beschreibung" %><br/>
                      <%= f.text_area :description, rows: 5, class:"form-control"%>
                    </div>
                    <div class="form-group">
                      <%= f.label "Ausschreibungskategorie" %><br/>
                      <%= f.collection_select :category_id, Category.all.order(:name), :id, :name, {}, class:"form-control" %>
                    </div>
                    <div class="form-group">
                       <input type="checkbox" name="my-checkbox2" checked>
                      <%= f.label "Sofort" %>
                    </div>
                    <slider>
                    <div class="form-group">
                      <%= f.label "Datum von-bis" %>
                      <%= f.text_field :date_from, class:"form-control"%>
                      <%= f.text_field :date_to, class:"form-control"%>
                    </div>
                    </slider>
                    <div class="form-group">
                       <%= f.label "Ausführungsort" %><br/>
                       <%= f.text_field :address1, class:"form-control" %>
                       <%= f.text_field :address2, class:"form-control" %>
                       <%= f.text_field :address3, class:"form-control" %>
                    </div>
                    <div class="form-group">
                       <input type="checkbox" name="my-checkbox1" checked>
                      <%= f.label "Aktiv" %>
                    </div>
                   <%= f.hidden_field :user_id %>
                   <%= f.hidden_field :geo_address, class:"form-control" %>
                   <%= f.hidden_field :longitude, class:"form-control" %>
                   <%= f.hidden_field :latitude, class:"form-control" %>
                    <div class="actions">
                       <%= f.submit "Speichern",  class:"btn btn-primary btn-lg"%>
                    </div>

                    <%= f.check_box :active, :id => "active", class:"form-control"%>
                    <%= f.check_box :asap, :id => "asap", class:"form-control"%>
                </div>
              </div>
              
                <div class="col-md-6">
                  <div class="panel-body">
                    <div id="map">
                      <div id="map-canvas"></div>
                    </div>
                </div>

            </div>
        </div>
    <% end %>                


<script>
    handler = Gmaps.build('Google');
    handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
      markers = handler.addMarkers([
        {
          "lat": <%= @bid.latitude %>,
          "lng": <%= @bid.longitude %>,
          "infowindow": "text"
        }
      ]);
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
      handler.setMapTypeId(google.maps.MapTypeId.SATELLITE);
});
</script>

<script>
    $("[name='my-checkbox1']").bootstrapSwitch('state',<%= @bid.active %>);
    $("[name='my-checkbox1']").on('switchChange.bootstrapSwitch', function () {
    $("#active").prop('checked',$("[name='my-checkbox1']").bootstrapSwitch('state'));
    });
    $("[name='my-checkbox2']").bootstrapSwitch('state',<%= @bid.asap %>);
    $("[name='my-checkbox2']").on('switchChange.bootstrapSwitch', function () {
    $("#asap").prop('checked',$("[name='my-checkbox2']").bootstrapSwitch('state'));
    if ($("[name='my-checkbox2']").bootstrapSwitch('state') == true) 
        {$("slider").hide()}
    else
        {$("slider").show()}    });
    if ($("[name='my-checkbox2']").bootstrapSwitch('state') == true) 
        {$("slider").hide()}
    else
        {$("slider").show()}
</script>