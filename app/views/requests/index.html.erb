

    <div class="panel-body">
    <div class="row">
      <div class="col-md-12">
        <div class="digg_pagination">
    		    <% if user_signed_in? %>
  		        <%= link_to searches_path :search_domain => "Kleinanzeigen", :controller_name => controller_name do %>
  		          <i class="btn btn-primary btn-xs glyphicon glyphicon-filter"></i>
  		        <% end %>
    		    <% end %>
            <%= will_paginate @requests, :container => false, previous_label: "<", next_label: ">" %>
        </div>
      </div>
    </div>
  </div>

  <div class="row">
      <%= header("Kleinanzeigen " + session[:rtype] + " <span class='badge'>" + @reqanz.to_s + "</span>", true) %>
  </div>
  
  <%= build_medialist("col-sm-6 col-md-4 col-lg-3", @requests, "requests", true) %>
  
